<%= form_for(@student) do |f| %>
<% if @student.errors.any? %>
    <div id="error_explanation">
      <h2>Encontramos alguns erros:<%= pluralize(@student.errors.count, "error") %> </h2>

      <ul>
      <% @student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label "Nome" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Número de registro" %><br>
    <%= f.text_field :register_number %>
  </div>
  <div class="field">
    <%= f.label "Status" %><br>
    <%= f.number_field :status %>
  </div>

  <div class="actions">
    <%= f.submit  %>
  </div>
<% end %>
