
        <div class="row">
            <div class="box">
                <div class="col-lg-12">
                  <div class="container">
                    <div class="col-md-1"></div>
                    <div id="table-responsive" class="col-md-10">
                      <h1>Listing classrooms</h1>
                      <table class="table table-striped table-bordered">
                        <thead>
                        <tr>
                          <th>Student</th>
                          <th>Course</th>
                          <th>Entry at</th>
                          <th colspan="3"></th>
                        </tr>
                        </thead>

                        <tbody>
                        <% @classrooms.each do |classroom| %>
                            <tr>
                              <td><%= classroom.student.name %></td>
                              <td><%= classroom.course.name %></td>
                              <td><%= classroom.entry_at.strftime('%A, %d %B %Y %H:%M:%S') %></td>
                              <td><%= link_to 'Mostrar', classroom %></td>
                              <td><%= link_to 'Excluir', classroom, method: :delete, data: {confirm: 'Você tem certeza?'} %></td>
                            </tr>
                        <% end %>
                        </tbody>
                      </table>
                      <%= link_to 'Novo', new_classroom_path,class: "btn btn-success" %>
                    </div>
                    <div class="col-md-1"></div>
                    <br/>
                  </div>
                </div>
            </div>
        </div>

    </div>
    <!-- /.container -->
